#ANSWERS

Q:What is the purpose of the various auxiliary methods populateList, populateFifoList, and ReverseLines.printReverse?
A: The methods help out with the tasks the require recursion.

Q:Why do these methods need to have arguments, and how does the argument change from one recursive call to the next?
A: In order to call themselves. The argument changes from the input to a new input.

Q:What are the time and space complexity of each of the populateList populateFifoList methods, as well as ReverseLines.printReverse?
A: O(n).

Q:Which of these methods can be implemented using while loops?
A: All of them.